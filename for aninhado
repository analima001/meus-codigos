for aluno in range(1,6):
    aluno=input("Qual seu nome?")
    soma=0
    for nota in range(1,5):
     nota=int(input("Qual foi sua nota?"))
     soma=soma+nota
     media= soma/4
    print(f"Aluno:{aluno} - Media:{media}")
